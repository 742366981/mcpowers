---
title: 爬虫分析规范
type: tech-spec
applies_to: [爬虫分析/逆向]
priority: recommended
version: 1.0
last_updated: 2026-07-08
---

# 爬虫分析规范

本文档定义爬虫目标的**分析方法论**，是《爬虫规范》（项目骨架）的配套文档。

> **适用范围**：本文档覆盖两件事——**接口分析**（抓包侦察、接口定位、请求/响应结构、反爬强度评估）与**逆向攻坚**（加密/签名参数还原）。项目工程骨架（目录结构、任务调度、部署等）不在此列，见《爬虫规范》。
>
> **文档定位**：`爬虫规范`（骨架/工程）→ 本文档`爬虫分析规范`（分析 + 逆向）。分析是入口，逆向是「分析发现参数被加密」后的延伸，故合并为一册、内部分层。

> **状态**：大纲骨架，各节内容待随实战案例逐步补充（标注 `TODO`）。

---

## 分类总览

| 部分 | 章节 | 定位 | 门槛 |
|:-----|:-----|:-----|:-----|
| **第一部分 接口分析** | 1–6 | 侦察：找到接口、看懂结构、评估反爬 | 通用，人人要做 |
| **第二部分 逆向攻坚** | 7–11 | 攻坚：加密/签名参数还原 | 进阶，遇加密才做 |
| **附录** | A–C | 工具、术语、案例库 | 参考 |

---

# 第一部分 · 接口分析（侦察）

> 目标：在**不做逆向**的前提下，摸清目标数据从哪个接口来、请求怎么构造、反爬有多强。绝大多数爬虫止步于此即可开工。

## 1. 分析流程总览

`TODO`：一张「明确目标数据 → 抓包定位接口 → 分析请求/响应 → 评估反爬 → 判断是否需逆向」的主流程图与决策树。

## 2. 抓包与工具

`TODO`：
- 浏览器 DevTools（Network / 断点 / Initiator 调用栈）
- 抓包代理（Charles / Fiddler / mitmproxy）
- APP 抓包（证书信任、SSL Pinning 初步判断）
- 工具选型建议（Web vs APP 各用什么）

## 3. 接口定位与筛选

`TODO`：从大量请求中定位「真正返回目标数据」的接口；XHR/Fetch 筛选；静态页 vs 动态接口的判断。

## 4. 请求结构分析

`TODO`：
- URL 与 Query 参数含义
- Header / Cookie / Token 的作用与来源
- Body（表单 / JSON / 二进制）
- 分页机制（page / cursor / 游标加密）
- 鉴权方式（Cookie / Bearer / 签名头）
- **加密参数的初步识别**（哪些参数不是明文 → 交给第二部分）

## 5. 响应结构分析

`TODO`：数据字段提取路径；响应加密/压缩的识别；空响应与风控响应的区分。

## 6. 反爬强度评估

`TODO`：限频规则、UA/指纹校验、Cookie 时效、验证码触发条件、风控信号——评估「能否用骨架规范里的代理/指纹/限速直接搞定」，还是必须逆向。
（应对手段的**工程实现**见《爬虫规范》代理/Session/浏览器指纹章节；此处只做**评估与识别**。）

---

# 第二部分 · 逆向攻坚（加密还原）

> 目标：当第一部分发现关键参数被**加密 / 签名 / 混淆**，无法直接构造时，还原其生成算法。门槛高、合规敏感，仅在必要时进行。

## 7. 加密参数定位

`TODO`：关键字搜索、XHR 断点、调用栈回溯、Hook 定位加密函数入口。

## 8. Web JS 逆向

`TODO`：
- 混淆类型识别与还原（变量混淆 / 控制流平坦化 / 字符串加密）
- AST 反混淆思路
- 补环境（补 `window`/`document`/`navigator` 等）

## 9. 参数还原与复现

`TODO`：
- Python 纯复现算法
- Node/execjs 直接调用原始 JS
- 还原结果的正确性校验方法

## 10. APP 逆向（进阶）

`TODO`：抓包绕过（SSL Pinning）、脱壳、frida Hook、so 层分析。按需展开。

## 11. 验证码与风控应对

`TODO`：常见验证码类型与应对思路、风控参数（设备指纹 / 行为轨迹）的生成还原。

---

# 附录

## A. 工具清单

`TODO`：抓包、调试、反混淆、Hook 等工具的统一清单与用途。

## B. 术语表

`TODO`：签名（sign）、混淆、补环境、脱壳、SSL Pinning、AST 等术语解释。

## C. 案例库

`TODO`：按「站点 / 加密类型」归档的实战案例，每例包含：目标、加密参数、定位过程、还原方案、可复用代码。

> **归档约定**：案例是本文档最有价值的部分，务必随每次逆向实战沉淀，避免手法遗失。
