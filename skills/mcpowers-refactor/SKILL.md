---
name: mcpowers-refactor
description: |
  重构 / 重构一下 / 代码重构 / 整理代码 / 重写代码 / 拆分代码 → 触发本技能。

  也覆盖口语："这段代码太乱了"、"代码太乱"、"代码太丑"、"代码难看"、"代码质量差"、"想抽离一下"、"想抽离一下公共逻辑"、"想抽方法"、"抽方法"、"提方法"、"拆方法"、"拆函数"、"拆类"、"拆分成小模块"、"拆成小函数"、"拆成小类"、"代码结构需要整理"、"代码结构太乱"、"结构不好"、"结构混乱"、"代码太啰嗦"、"代码太复杂"、"代码太长了"、"太啰嗦了要简化"、"要简化"、"简化一下"、"抽象一下"、"我想重构一下"、"我想整理一下代码"、"帮我把这段代码拆开"、"帮我整理一下"、"帮我重构一下"、"代码太耦合了"、"耦合太重"、"解耦"、"去耦合"、"解除耦合"、"整理一下代码"、"整理一下结构"、"提取公共方法"、"提取公共逻辑"、"提取公共"、"清理一下代码"、"分离关注点"、"封装一下"、"代码要整理"、"代码要重构"、"可读性差"、"可读性不好"、"可读性需要提升"、"命名混乱"、"命名不清晰"、"命名不规范"、"逻辑太复杂"、"逻辑混乱"、"重新组织"、"重新组织代码"、"把代码改清晰"、"代码清晰"、"代码可读"、"代码冗余"、"代码重复"、"去重"、"DRY"、"solid"、"SOLID"、"clean code"、"clean up"。

  中英混输："refactor"、"simplify"、"clean up"、"extract"、"extract method"、"restructure"、"rewrite"、"reorganize"、"solid"。

  边界（防误触发）：**修 bug、改行为** → `mcpowers-bugfix`（重构禁改行为）；**新增功能** → `mcpowers-feat`；**性能优化** → `mcpowers-optimize`。

  保行为重构：先固化测试 → 小步改 → 每步验证。
  **禁止**"顺手优化"导致行为变化、**禁止**没有测试保护网就重构。
---

# mcpowers-refactor（重构）

> 借鉴自 superpowers（TDD 保护网思想）。
> **核心**：行为不变，只改结构。**没有测试保护网不做重构**。

---

## 编排

本技能按顺序调用以下方法层技能 + 规范：

| 步骤 | 调用对象 | 类型 | 触发条件 | 失败时 |
|:-----|:---------|:-----|:---------|:-------|
| 1 | `mcpowers-tdd`（先补测试保护网） | 方法 | 测试覆盖不足时 | 中断，禁止无保护网重构 |
| 2 | 规范组（`mcpowers-spec-index`） | 规范 | 涉及栈规范时 | 提示加载 |
| 3 | 行为不变循环 | 内联 | 每步验证 | 不一致立即回滚 |

**保护路径**：`mcpowers-shared/`、`mcpowers/`、`hooks/` 三个目录的写操作触发前确认。

**铁律**：行为不变，禁止顺手"修 bug"或"加功能"（重构 ≠ bugfix ≠ feat）。

---

## 触发即执行

### 1. 加载规范
- Read `mcpowers-shared/mcpowers-spec-index/SKILL.md`
- 加载：
  - `mcpowers-shared/docs/技术规范/代码规范.md`（**必读**，SOLID/KISS/DRY/YAGNI）
  - `mcpowers-shared/docs/技术规范/代码同步修改规范.md`（**必读**）
  - 对应栈规范

### 2. 评估测试覆盖（HARD-GATE）
- 检查目标代码**是否有测试**
- 测试覆盖**不足** → 调 `mcpowers-tdd` **先补测试**
- 测试覆盖足够 → 进入第 3 步
- **禁止**没有测试保护网就重构（改了不知道改没改对）

### 3. 识别重构目标
按代码异味分类：

| 代码异味 | 重构手法 |
|:---------|:---------|
| 长函数 | 提取函数 |
| 大类 | 拆分类 |
| 重复代码 | 提取公共部分 |
| 复杂条件 | 用多态 / 策略模式替换 |
| 紧耦合 | 依赖注入 / 接口隔离 |
| 神类（万能类） | 单一职责拆分 |
| 长参数列表 | 引入参数对象 |

### 4. 小步重构
- **每步改动都要小**（一个手法一次）
- 每步改动后**立即跑测试**
- 测试失败 → 立即回滚，不继续
- 测试通过 → 进入下一步

### 5. 完成后验证
- 全量测试通过
- 全量 lint / type check 通过
- 行为完全一致（外部接口、内部逻辑、边界情况）

---

## 重构 vs 修 bug / 加功能

| 场景 | 用什么技能 |
|:-----|:-----------|
| 改 bug | `mcpowers-bugfix` |
| 加新功能 | `mcpowers-feat` |
| 改需求 | `mcpowers-requirement-change` |
| 改结构不改行为 | `mcpowers-refactor`（本技能） |
| 修 bug 时顺手重构 | **禁止**（拆成两次：先 bugfix 后 refactor） |

---

## 反模式（禁止）

- ❌ 没测试就重构
- ❌ 重构时改行为（这是 bugfix 不是 refactor）
- ❌ 一步到位大改（应小步）
- ❌ 改完不跑测试
- ❌ 顺手"优化"无关代码
- ❌ 重构和加功能混在一起

---

## 收尾

- 调 `mcpowers-code-review` 自审
- 调 `mcpowers-git-commit` 提交
- commit 信息：`refactor(模块): 简要说明`（用 `refactor` 而非 `feat`/`fix`）
