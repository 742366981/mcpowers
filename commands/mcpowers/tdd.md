---
---
description: 写代码 / 修 bug 时强制 TDD 循环：先写失败的测试 → 写最少的代码使测试通过 → 重构。被 `mcpowers-feat` / `mcpowers-bugfix` 调用。
---

# /mcpowers:tdd

调 mcpowers-tdd 技能。

Read `~/.claude/skills/mcpowers-tdd/SKILL.md` 加载完整流程，然后按用户需求执行。
