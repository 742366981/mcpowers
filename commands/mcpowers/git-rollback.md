---
description: 用户要"回滚/rollback/撤销/恢复"时触发。安全回滚代码版本，区分 `git revert`（保留历史，**安全**）和 `git reset`（改历史，**危险**）。默认推荐 revert，避免破坏共享历史。
---

# /mcpowers:git-rollback

调 mcpowers-git-rollback 技能。

Read `~/.claude/skills/mcpowers-git-rollback/SKILL.md` 加载完整流程，然后按用户需求执行。
