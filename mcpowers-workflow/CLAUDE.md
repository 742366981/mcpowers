# AI 项目配置

本文档是 AI 项目的配置文件，定义 AI 在本项目中的行为约束和执行规范。Claude Code 工具自动加载此文件。

> **说明**：不同 AI 工具可能使用不同配置文件（`CLAUDE.md` 或 `AGENTS.md`），两者内容保持一致。
>
> **重要**：修改规范时，**必须同时更新两个文件**，确保内容一致。

---

## 会话首次行为

当会话开始后收到第一条消息时，AI 必须先执行 Step 1 的完整流程，再响应用户需求。

> **会话开始后的首条消息必须触发 Step 1，完成项目情况汇报后才能回答**
>
> **后续消息正常响应，不再重复执行 Step 1**
>
> **读取的所有规范必须严格遵守，不得违背其中任何规定**

---

## 0. 速查表（每次任务前必须对照）

### 强制执行项
- [ ] **修改前**：必须先分析影响、询问确认
- [ ] **修改时**：必须遵守 `docs/技术规范/代码同步修改规范.md`
- [ ] **完成时**：必须立即 commit（禁止多个操作后才 commit）
- [ ] **接口开发**：必须先写 docstring，再写实现
- [ ] **代码注释**：函数/类/复杂逻辑必须有完整注释
- [ ] **临时文件**：必须放在 `temp/` 目录，用完即删
- [ ] **细节记录**：用户告知关键信息时，必须立即记录到 `docs/细节记录/`

### 禁止项
- ❌ 未经用户确认直接修改任何代码/文档
- ❌ 先写代码后补文档
- ❌ 只 commit 代码不 commit 文档
- ❌ 多个文档重复定义相同内容
- ❌ 在项目目录创建临时文件

---

## 1. 强制行为约束（红线）

| 红线行为 | 违规后果 |
|:---------|:---------|
| 未经确认直接修改代码/文档 | 用户有权要求回滚 |
| 先写代码后补文档 | 视为不合格 |
| 多个操作后才 commit | 视为不规范 |
| 只 commit 代码不 commit 文档 | 视为不规范 |
| 发现重复定义未处理 | 视为不合格 |
| 代码注释不完整 | 视为不合格 |
| 临时文件不清理 | 视为不规范 |
| 违反 SOLID/KISS/DRY/YAGNI | 视为不合格 |

---

## 2. 细节记录机制

### 触发条件
- 用户主动告知关键信息时 → 立即记录
- 用户告知的场景/限制/注意事项 → 记录并后续遵守

### 记录位置
`docs/细节记录/`

### 记录格式
```markdown
## 用户告知的关键信息

| 日期 | 内容 | 用途/关联 |
|-----|------|----------|
| 2026-05-09 | 用户告知的XXX | 用于XXX功能开发 |
```

### 开发前检查
- 修改某功能前 → 检查是否有该功能相关的用户告知记录
- 有记录 → 必须遵守记录内容
- 无记录 → 正常开发

---

## 3. 项目结构

| 类型 | 路径 | 说明 |
|:-----|:-----|:-----|
| 虚拟环境 | `../venv/{项目名}-env/Scripts/python` | Windows：`Scripts/python`，Unix：`bin/python` |
| 原始需求 | `docs/原始需求/` | 用户提供的需求材料 |
| 需求文档 | `docs/需求文档/` | 标准化需求 |
| 设计文档 | `docs/设计文档/` | 架构和技术方案 |
| 技术规范 | 技术规范文件位于共享技能目录 | AI 从技能读取，不复制到项目 |
| API 文档 | `docs/API文档/` | 接口详细设计，**必须包含完整示例** |
| 细节记录 | `docs/细节记录/` | 用户告知的关键信息 |
| 临时文件 | `temp/` | 用完即删 |

---

## 4. 技术规范

规范文件位于共享技能目录：`~/.claude/skills/mcpowers-shared/docs/技术规范/`

### 通用规范（所有项目必须）

| 规范 | 说明 |
|:-----|:-----|
| API规范.md | API设计 |
| 数据库规范.md | 数据库设计 |
| 缓存规范.md | 缓存使用 |
| 定时任务规范.md | 定时任务 |
| 部署规范.md | 项目部署 |
| Git规范.md | Git使用 |
| 开发环境规范.md | 项目初始化 |
| 代码同步修改规范.md | 变更同步 |
| 代码规范.md | 代码质量 |
| 设计规范.md | 文档编写 |
| 文档编写规范.md | 文档标准 |
| 测试规范.md | 测试 |
| 细节记录规范.md | 细节记录 |

### 技术锁规范（按项目类型）

| 规范 | 适用项目 |
|:-----|:---------|
| Flask后端规范.md | Flask 后端项目 |
| Vue前端规范.md | Vue 前端项目 |
| 爬虫规范.md | 爬虫项目 |

---

## 5. 执行流程

### 完整流程（7步）

| 阶段 | 说明 |
|:-----|:-----|
| Step 0 | 项目初始化（仅首次/缺失文件时） |
| Step 1 | 识别核心规范（强制） |
| Step 2 | 查找需求 |
| Step 3 | 确认设计文档 |
| Step 4 | 代码执行 |
| Step 5 | 细节记录（持续） |
| Step 6 | 三阶段同步（强制） |
| Step 7 | 执行测试（代码完成后） |

### Step 1 完整流程（共7步）

| 步骤 | 内容 | 完成标志 |
|:----:|:-----|:---------|
| 1.1 | 扫描规范目录 | 输出扫描结果 |
| 1.2 | 确定项目类型 | 输出项目类型 |
| 1.3 | 识别技术锁规范 | 输出匹配的规范 |
| 1.4 | 读取所有适用规范 | 每个输出`✓ 已读取` |
| 1.5 | 规范遵守承诺 | 输出承诺内容 |
| 1.6 | 执行规范检查 | 每个规范输出检查结果 |
| 1.7 | 汇报项目情况 | 输出完整汇报（含细节记录） |

---

## 6. 三阶段同步机制

需求、设计、代码三者必须保持同步，任何变更都必须向上下游传递。

| 同步方向 | 触发条件 | 同步内容 |
|:---------|:---------|:---------|
| **正向同步** | 需求变更 | 原始需求 → 标准需求 → 设计文档 → 代码 |
| **逆向修正** | 代码实现与设计文档不符 | 代码实现 → 更新设计文档 |

### 禁止行为
- ❌ 代码与设计文档不一致
- ❌ 设计文档与需求不一致
- ❌ 变更只改一处
- ❌ 跳过设计文档直接改代码
