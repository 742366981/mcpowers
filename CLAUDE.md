# AI 项目配置

本文档定义 AI 在本项目中的行为约束。

---

## 规范路径

| 类型 | 路径 | 使用方式 |
|:-----|:-----|:---------|
| 技术规范 | `~/mcpowers/docs/技术规范/` | AI 直接读取 |
| 产品设计 | `~/mcpowers/docs/产品设计/` | AI 直接读取 |
| 项目文档 | `docs/` | 按需创建 |

> 注：规范由 AI 从技能目录直接读取，不复制到项目本地

---

## 执行流程

| 阶段 | 输入 | 输出 |
|:-----|:-----|:-----|
| 产品设计 | 用户想法 | `docs/原始需求/` |
| 需求转换 | 原始需求 | `docs/需求文档/` |
| 设计生成 | 需求文档 | `docs/设计文档/` |
| 代码生成 | 设计文档 | 代码实现 |

---

## 强制要求

| 要求 | 说明 |
|:-----|:-----|
| 文档先行 | 接口先写 docstring，再写实现 |
| 细节记录 | 用户告知的关键信息 → `docs/细节记录/` |
| 立即 commit | 每个任务完成后立即 commit |
| 三阶段同步 | 需求 → 设计 → 代码 保持一致 |

---

## 目录按需创建

| 目录 | 时机 |
|:-----|:-----|
| `docs/原始需求/` | 有原始需求时 |
| `docs/需求文档/` | 有标准化需求时 |
| `docs/设计文档/` | 有设计需求时 |
| `docs/API文档/` | 有接口开发时 |
| `docs/细节记录/` | 有重要细节时 |

---

## 行为红线

- 未经确认直接修改 → 用户有权要求回滚
- 先写代码后补文档 → 视为不合格
- 多个操作后才 commit → 视为不规范
- 代码与设计文档不一致 → 视为不合格
