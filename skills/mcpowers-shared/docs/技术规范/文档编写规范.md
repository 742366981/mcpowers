---
title: 文档编写规范
type: tech-spec
applies_to: [写文档]
priority: recommended
version: 1.0
last_updated: 2026-07-08
---

# 文档编写规范

本文档定义文档编写标准，适用于所有项目。

---

## 1. 章节编号格式（强制）

**所有文档的章节标题必须使用阿拉伯数字编号**，禁止使用中文数字：

| 错误 | 正确 |
|:-----|:-----|
| `## 一、xxx` | `## 1. xxx` |
| `## 十、xxx` | `## 10. xxx` |
| `## 第1章` | `## 1. xxx` |

**格式要求**：
- 一级标题：`## 1.`
- 二级标题：`### 1.1`
- 阿拉伯数字 + 半角点号 + 空格

**示例**：
```markdown
## 1. 项目概述

## 2. 功能需求

### 2.1 用户管理
```

---

## 2. 规范文档标题标注（强制）

**规范文档**（docs/技术规范/ 下的所有文档）的章节标题标注规则：

### 2.1 通用规范文档

| 章节类型 | 示例 | 是否标注强制 |
|:---------|:-----|:-------------|
| 描述性章节 | `## 1. 概述`、`### 1.1 适用范围` | ❌ 不标注 |
| 规范性质章节 | `## 2. 命名规范（强制）`、`### 3.1 禁止行为（强制）` | ✅ 标注 |

> **判断标准**：章节内容是否包含"必须"、"禁止"、"强制要求"等规范性质的要求

适用文档：`docs/技术规范/` 下除技术锁定类规范外的所有文档。

### 2.2 技术锁定类规范

以特定技术命名的规范文档（如 Vue前端规范.md、Flask后端规范.md、爬虫规范.md 等），因其内容本身就是技术要求，**所有章节均可标注强制**，无需逐章判断。

**非规范文档**（需求文档、设计文档等）**不需要标注强制**。

---

## 3. 章节组织原则（强制）

**相同类型的规范必须放在同一章节，禁止分散**：

| 错误做法 | 正确做法 |
|:---------|:---------|
| 路径规范放在第3章，下拉响应格式放在第9章 | 路径规范和下拉响应格式都放在第3章 |
| 同一类接口的规范分散在多个章节 | 同一类接口的规范集中在一个章节 |
| 相关字段说明分散在不同章节 | 相关字段说明集中在同一章节 |

**章节编号必须连续，不得跳跃**：
- ❌ 3.2 → 3.4（缺少3.3）
- ✅ 3.1 → 3.2 → 3.3

---

## 4. 不冗余原则（强制）

**禁止在多个文档中重复定义相同内容**。每个知识点只在一个地方定义，其他地方通过引用链接。

| 错误做法 | 正确做法 |
|:---------|:---------|
| 在多个文档中写相同的概念定义 | 概念只定义一次，其他地方引用 |
| 在 AI操作规范 中写详细的技术细节 | 详细内容写在技术规范中，AI操作规范只引用 |
| 在多个地方复制粘贴相同的说明 | 写一份，其他地方引用 |

---

## 5. 不重复定义原则（强制）

**同一个概念、规则、流程只定义一次**，无论是在单个文档内部还是多个文档之间。

**单个文档内部**：
- 同一概念只在文档的一处定义
- 相似内容合并为一处，避免分散描述
- 避免"在第3章讲过，第5章又讲一遍"

**多个文档之间**：
- 详细内容只在专属文档中定义，其他文档只引用
- 避免在多个文档中复制相同的说明

---

## 6. 内容完整原则（强制）

**每个文档的内容必须是完整的**，避免：

- 文档内容残缺，需要参照其他文档才能理解
- 关键信息缺失，需要猜测或询问
- 示例不完整，无法直接使用

---

## 7. 适用范围标注规范（强制）

**同一文档包含多种技术时，必须在章节标题标注适用范围**。

| 标注方式 | 示例 |
|:---------|:-----|
| 技术无关 | `## 2. ORM框架（MySQL/SQLAlchemy）` |
| 纯通用 | `## 5. 反向代理（通用）` |
| 多技术并列 | `## 3. 缓存（Redis/Memcached）` |

**规则**：
- 章节内容仅涉及单一技术 → 在标题标注该技术
- 章节内容通用 → 在标题标注"（通用）"
- 章节内容涉及多种技术 → 在标题标注所有适用技术

**示例**：
```markdown
## 2. 数据库规范

### 2.1 ORM框架（MySQL/SQLAlchemy）
### 2.2 MongoDB（规划中）

## 3. 缓存规范

### 3.1 Redis（通用）
### 3.2 Memcached（规划中）

## 4. 部署规范

### 4.1 Docker（通用）
### 4.2 Kubernetes（规划中）
### 4.3 Python镜像（Python）
```

**好处**：
- 用户一看标题就知道内容是否适用
- 避免通用规范被误认为适用于所有技术
- 新增技术时只需在标题添加，无需重构文档

---

## 8. 代码示例使用规范（强制）

**通用规范文档**：不应包含代码实现，仅描述规范要求

| 规范类型 | 代码示例要求 | 说明 |
|:---------|:------------|:-----|
| 通用规范 | ✅ 仅作参考 | 代码示例仅辅助理解，不是完整实现 |
| 技术锁定规范 | ✅ 必须 | 可包含该技术的完整代码实现 |

**通用规范示例**（代码规范.md、数据库规范.md 等）：
- ✅ 可以：描述性规范、原则、要求
- ✅ 可以：多语言对比示例（如 Python/JavaScript）
- ❌ 不应：完整的代码实现（会降低通用性）

**技术锁定规范示例**（Flask后端规范.md、Vue前端规范.md 等）：
- ✅ 必须：包含该技术的完整代码实现
- ✅ 必须：包含该技术的配置示例

---

## 9. 编写检查清单（强制）

编写新文档或更新文档时，对照检查：

- [ ] 是否有内容在其他文档中重复定义？
- [ ] 是否有内容应该引用而非复制？
- [ ] 文档内容是否完整，可以独立使用？
- [ ] 示例是否完整可运行？
- [ ] 引用路径是否正确？
- [ ] 同一文档内多技术是否已标注适用范围？
- [ ] 通用规范是否只包含代码示例而非完整实现？

---

## 附录

### A. 相关文档

| 文档 | 位置 |
|:-----|:-----|
| AI操作规范 | `../AI操作规范.md` |
