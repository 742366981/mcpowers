# mcpowers 历史教训归档

> **本文件是只读归档**，记录 v2.0.3 → 当前版本所有真实复盘。
> **禁止**在 `CLAUDE.md` / `README.md` 顶层文档复制本文件内容；如需引用请用相对链接。
> 维护规则：每个 release 后在文末追加新版本段；不再修改历史段；文末维护索引表。

---

## 索引

| 版本 | 主题 | 关联权威规范 | CHANGELOG 链接 |
|:-----|:-----|:------------|:--------------|
| [v2.0.3 → v2.0.4](#v203--v204-description-截断风险修复) | description 多行块截断 + 4 段式单行紧凑化 | `AI操作规范.md` §Skill Description 规范 | — |
| [v2.6.0](#v260-新增顶层规范-日志规范md) | 新增顶层规范（横跨 9+ 文件同步） | `日志规范.md` / `mcpowers-spec-index` | — |
| [v2.9.5](#v295-强化单技能能力底线-6-类文件同步) | 单技能能力强化 = 6 类文件同步底线 | `爬虫工具与抓包规范.md` §2.5/§2.7/§3.0/§3.4.5 | — |
| [v2.10.0](#v2100-集成第三方-cli--bb-browser) | 集成第三方 CLI（bb-browser），可选依赖原则 | `爬虫工具与抓包规范.md` §2.5.5 | — |
| [v2.11.1](#v2111-bb-browser-实操补全) | bb-browser 完整实操链路（安装 / 启动 / 验证 / 失败处理） | `爬虫工具与抓包规范.md` §2.5.5.0/§2.5.5.1/§2.0.5.1 | — |
| [v2.12.0](#v2120-真实可用性验收门禁阶段-55) | 阶段 5.5 真实可用性验收 + 生命周期 7 分类 | `爬虫分析规范.md` §1.1/§9.4 | — |
| [v2.13.0](#v2130-逆向分层与浏览器所有权铁律) | 逆向 7 专项拆分 + 浏览器所有权铁律 | `爬虫分析规范.md` §1.3 | — |
| [v2.14.0](#v2140-爬虫分析规范拆分-7-册--1-配套) | 爬虫规范拆 7 册 + 1 配套 | `爬虫工具与抓包规范.md` / 7 个平台专项 | — |
| [v2.15.0](#v2150-协作模式-b-工具化) | 协作模式 B 工具化（user-action-recorder） | `爬虫工具与抓包规范.md` §8 | — |
| [v2.16.0](#v2160-抓包失败-7-层诊断--curl-快速帮助) | 抓包失败 7 层诊断 + cURL 快速帮助 | `爬虫分析规范.md` §3.0.7/§3.0.8 | — |
| [v2.17.0](#v2170-模块产物封装形式标准化) | 类式封装 + SRP + 零前置参数 + quick_test | `爬虫分析规范.md` §9.4.6 | — |
| [v2.18.0](#v2180-drissionpage-全场景默认化) | DrissionPage 全场景默认 + 接管语法对照 | `爬虫工具与抓包规范.md` §2.1/§3.5/§3.6/§3.9/§7.2 | — |
| [v2.18.1](#v2181-drissionpage-反检测描述精准化) | DrissionPage 反检测描述精准化 + 4 参考链接 | `爬虫工具与抓包规范.md` §2.1/§3.7 | — |
| [v2.18.2](#v2182-真实接管链路实测暴露-4-个-bug) | 真实接管链路 1 次实测暴露 4 个 bug | `爬虫工具与抓包规范.md` §3.7 | — |
| [v2.19.0](#v2190-逆向工作区与-web-协作会话强制起手式--指纹审计) | 逆向工作区 + Web 协作会话 + 指纹审计 | `爬虫分析规范.md` §3.0.2 | — |
| [v2.20.0](#v2200-项目独立端口) | 项目独立端口（pick_free_port + 端口池 fallback） | `爬虫分析规范.md` §3.0.6 | — |
| [v2.21.0](#v2210-web-会话派生产物自动生成--app-录制选型调研) | Web 会话派生产物 + App 录制选型调研 | `爬虫工具与抓包规范.md` §8.8 | — |
| [v2.21.2](#v2212-标准化-web-浏览器监控模板) | 标准化 Web 浏览器监控模板（开箱即用 + 7 类配置正确性防御 + run_js JS 异常保护） | `爬虫工具与抓包规范.md` §7.2 | — |

---

## v2.0.3 → v2.0.4 description 截断风险修复

- **v2.0.3**：一次 description 大改版用 `|` 多行块，**3 个文件超 1024c 被截断**（code-review 1091c / brainstorm 1050c / bugfix 1027c），尾部"出错了"、"闪退"、"帮我审"等高频触发词全部失效。
- **v2.0.4**：全部改为单行紧凑版，**L1 description 总预算从 ~9000c 降到 5986c（-34%）**，**0 个文件超 800c**。

## v2.6.0 新增顶层规范（日志规范.md）

- **v2.6.0**：新增 `日志规范.md` 时，只改 `mcpowers-spec-index` 查表 + CLAUDE.md/README.md 数字声明**不够**。还必须同步改造至少 5 个相关编排：
  - `mcpowers-init`：注入日志基础设施（`utils/loggings.py` + `utils/request_log.py` + `log/` 目录）
  - `mcpowers-feat`：常见规范组合 + 架构设计阶段必读 + 完成后自检清单
  - `mcpowers-bugfix`：调查方法加"按 `request_id` / `trace_id` 串联日志"
  - `mcpowers-deploy`：上线 checklist 加"日志收集方案 + 大内容策略 + 轮转保留"
  - 否则规范进了体系但**不触发**，等于没加。
- **结论**：新增顶层规范是**横跨 9+ 文件**的改动（1 新规范 + 1 spec-index + 5 技能编排 + 2 文档 + 2 版本文件），不能省任何一处。

## v2.9.5 强化单技能能力（底线 6 类文件同步）

- **v2.9.5**：升级 `mcpowers-crawler-reverse` 时发现，单技能能力强化（新增 §2.7 弹窗检测 + §3.0 协作模式 + §3.4.5 置信度）也必须横跨**至少 6 类文件**同步：
  1. 主技能 SKILL.md（description + 阶段 2 SOP 重构 + 阶段 4.5 强化 + 自检清单 + 反模式）
  2. 共享规范文档（§2.5 + §2.7 + §3.0 + §3.4.5 + 附录 D）
  3. spec-index 查表（加 v2.9.5 子节链接）
  4. 新增工具脚本（`popup-handler.py`，与字典库对应）
  5. 2 顶层文档（CLAUDE.md + README.md 注释）
  6. 3 版本文件（`plugin.json` + `marketplace.json` × 2）
- **结论**：**单技能强化 ≠ 单文件改动**。CLAUDE.md "文档同步约束" 提到的 6 类文件是**底线**，还要算上共享规范和新增脚本。**凡是用户可见的能力变化，都必须按这个清单同步**。

## v2.10.0 集成第三方 CLI（bb-browser）

- **v2.10.0**：`bb-browser`（[epiral/bb-browser](https://github.com/epiral/bb-browser)）是首个被正式集成到爬虫逆向 SOP 的第三方 CLI / MCP server。外部工具接入不能只改主技能，必须同步维护 6 类文件：`CLAUDE.md`、`README.md`、`SKILL.md`、`scripts/check-readme-sync.sh` 校验边界、`.claude-plugin/plugin.json`、`.claude-plugin/marketplace.json`；同时还要同步共享规范 `last_updated` frontmatter、§2.5.5 详情页、spec-index 查表行、相关工具脚本的 docstring 注释（popup-handler.py 加 `DEFAULT_BB_BROWSER_PROBE` 提示常量）。
- **关键设计**：bb-browser 必须是**可选依赖**。未安装、daemon 未运行、adapter 未命中或版本不兼容时，v2.9.5 的 Chrome CDP + Playwright + popup-handler.py 原链路必须**完全可用**，不得把第三方 CLI 变成硬依赖；任何缺 bb-browser 就会失败的写法都是反模式。
- **关键经验**：技能 description 接近 1024c 时，必须**先压缩重复触发词和能力描述**（从长流程压缩为"骨架 / 口语 / 中英 / 边界"4 段式），再增加 `bb-browser`、`site adapter`、`MCP server`、`登录态保留` 等新触发词；实施前后都要用 Python 检查字符数，预算严格控制在 800c 以内，避免尾部高频词被截断。
- **关键工具设计**：`popup-handler.py` 的职责边界是 DOM 弹窗处理，**不引入 `probe_browser_daemon()` 函数**（避免引入 Node / subprocess 依赖），daemon 探测由 SKILL.md §2.0 SOP 层说明；脚本只新增 `DEFAULT_BB_BROWSER_PROBE` 提示常量声明职责边界。

## v2.11.1 bb-browser 实操补全

- **v2.11.1**：v2.10.0 集成 bb-browser 后真实用户复盘发现 3 类实操缺失——**安装指引未指定安装位置与 Node 版本要求**、**daemon 与 Playwright 共享 Chrome CDP 的启动顺序与端口分配不明确**、**adapter 调用失败判定与结果合并规则缺失**。这 3 类问题属于 v2.10.0 文档化的「已知风险」未在 SOP 落地。
- **关键修复**：补 3 个子节——SKILL.md §2.5.5.0 安装与 MCP 配置（含双轨 fallback：全局 + 本地+npx，规避 Issue #6 SSL 错误）、§2.5.5.1 daemon + Playwright 共享 Chrome CDP 实操（含启动顺序、端口分配、4 步验证）、§2.0.5.1 adapter 失败判定与结果合并（含 4 类失败判定、调用日志格式、合并冲突优先级、api-inventory.md 加「来源」列）。
- **关键经验**：第三方 CLI 集成**不只是「可选用法 + 反模式」**，必须包含**完整实操链路**（安装 → 启动 → 验证 → 失败处理 → 结果合并）；任何「链接到 README 自己看」的写法都是反模式，因为用户复盘时不可能跳出去读外部文档。
- **版本策略**：v2.10.0 → v2.11.1 是 patch bump（文档完善 + bug fix），不是 minor（无新功能），符合 CLAUDE.md 版本管理规则。
- **docstring 注释同步**：popup-handler.py 的 `DEFAULT_BB_BROWSER_PROBE` 常量在 v2.11.1 中仍是「最小改动 + 职责声明」，不引入 daemon 探测代码（守住 YAGNI 边界）。

## v2.12.0 真实可用性验收门禁（阶段 5.5）

- **v2.12.0**：真实用户复盘发现，接口 `[🎯]`、≥ 3 组 sign 一致、单次 HTTP 200 和模块文件生成只能分别证明接口语义、算法或单次协议有效，**不能证明模块真的可用**；旧流程缺少阶段 5 → 6/7 的硬门禁，容易在重复调用、跨会话、一次性 token 或并发尚未验证时过早创建完整骨架。
- **关键修复**：新增 SKILL.md 阶段 1 交付形态确认（纯协议 / 半自动化 / 纯自动化），明确 RPC 只是阶段 4 的逆向实现方式；新增阶段 5.5 +《爬虫分析规范》§1.1/§9.4，强制从模块公开入口验证业务语义、至少 2 组输入/合计 ≥ 5 次、至少 2 个 session/冷启动、原报文重放、动态参数重生成、跨 session、TTL 和有界并发 2 → 5；统一产出 `verification-report.md`，只有 `PASS` 才能进入阶段 6/7。
- **生命周期分类**：关键状态必须归类为 `reusable` / `per-request` / `single-use-token` / `session-bound` / `time-bound` / `challenge-bound` / `unknown`；`unknown` 禁止冒充可复用，原报文无法重放不等于逆向失败，模块能按生命周期持续生成有效新报文才是关键。
- **YAGNI 边界**：轻量模块不引入 Session 池、代理池、Redis 队列或任务调度，但必须包含完成一次真实业务调用所需的最小 token/challenge/session 生命周期，或通过清晰接口显式注入；禁止把抓包临时 token/Cookie/nonce 写进常量。
- **安全边界**：并发仅做小规模 2 → 5 可用性验证，不做压力测试；出现 429、验证码增加、账号提示、目标异常或授权/条款边界时立即停止。
- **版本策略**：v2.11.1 → v2.12.0 是 minor bump，因为新增了用户可见的验收阶段、生命周期分类和并发门禁，不是纯文档修正。

## v2.13.0 逆向分层与浏览器所有权铁律

- **v2.13.0**：原 `mcpowers-crawler-reverse` 同时装载 Web、Android/iOS App、Flutter/Hybrid、小程序和公共验收，导致平台无关任务也加载全部工具链。现拆为统一入口 + Web + App 二级入口 + Android/iOS/Flutter/Hybrid/小程序 7 个专项；公共前置、阶段 5.5、生命周期和落地门禁仍由统一入口唯一维护。
- **分类原则**：先按载体，再按运行时；Kotlin/Java/JNI 归 Android，Swift/Objective-C 归 iOS，uni-app/RN/Cordova/Capacitor/WebView 归 Hybrid，单个平台小程序暂不继续平铺。专项只交标准证据，禁止复制公共验收或自行宣布 PASS。
- **浏览器所有权铁律**：通过 CDP/WebView/daemon 接管的 browser/context/page/tab 和外部 daemon 一律视为外部所有。正常收尾、异常和回退都不得 `browser.close()`、`context.close()`、关闭既有 page、kill 用户 Chrome 或 stop 外部 daemon；纯协议验收通过停止依赖并独立调用完成，绝不能为了测试而关闭用户本身的浏览器。
- **门禁**：`check-readme-sync.sh` 增至 12 类检查，新增 reverse 拓扑/公共合同与外部资源所有权断言，防止后续合并回单体或恢复危险清理逻辑。
- **版本策略**：新增 7 个用户可见场景技能与二级路由，`2.12.0 → 2.13.0` 为 minor bump。

## v2.14.0 爬虫分析规范拆分 7 册 + 1 配套

- **v2.14.0**：原 `爬虫分析规范.md` 单文件 1710 行 / 81KB，§2 抓包与工具独占 584 行 / 34%，单一文件按需加载效率低；reverse 技能已按平台拆为 7 个专项，对应规范文档也应**按相同拓扑拆分**。
- **关键修复**：保持主《爬虫分析规范.md》为公共方法论（§1 流程/§3-§6 接口分析/§9.4 验收/§10.9 指纹交接/§11 风控），新增 7 个独立规范文件——`爬虫工具与抓包规范.md`（公共配套：抓包/自动化/CDP/弹窗字典/bb-browser/协议层）+ `爬虫Web逆向规范.md` + `爬虫Android逆向规范.md` + `爬虫IOS逆向规范.md` + `爬虫Flutter逆向规范.md` + `爬虫Hybrid逆向规范.md` + `爬虫小程序逆向规范.md`。
- **所有权铁律跨册落地**：§2.5.2.1「外部接管资源不可关闭」原文保留在主《爬虫分析规范》§1.3 作为唯一权威引文；实操步骤放工具册 §3.5.1；Web 逆向册、Hybrid 册、小程序册各自按需引用主册 §1.3；不允许各平台专项重复定义铁律。`check-readme-sync.sh` §12 OWNERSHIP_FILES 加入新册路径，仍校验 `外部接管资源不可关闭` 字符串。
- **跨文件职责边界**：工具册唯一维护抓包工具栈、CDP 接管、弹窗字典 8 类、bb-browser MCP；各平台专项只维护本平台逆向方法（脱壳/SSL Pinning/IPA/blutter/JSBridge 等）；主册只维护公共方法论；不允许相同内容在两个规范文件出现（包括章节级别）。
- **同步面**：本次横跨 25 个文件改动。
- **版本策略**：新增 7 册用户可见规范 = minor bump，`2.13.0 → 2.14.0`。

## v2.15.0 协作模式 B 工具化

- **v2.15.0**：协作模式 B（用户操作 + AI 抓包）自 v2.9.5 起只有口头协议，无工具支撑；真实用户复盘发现 AI 自动化分析页面按钮操作耗时过长，期望"人操作 + AI 监控抓包 + 记录操作流 + 沉淀可重放脚本"。
- **关键设计**：选型 A-最小（5h）只做录制 + 重放，**不**做 AI 智能 selector、失败自愈、HAR diff、数据点标注（守 YAGNI，避免重复造 popup-handler 字典维护的轮子）。
- **关键技术约束**：Playwright `record_har_path` 在 `connect_over_cdp` 模式不可用（只对 `launch()` / `launch_persistent_context()` 创建的 context 生效），本模块手写 `page.on("request"/"response")` 落 JSONL 格式 HAR。
- **职责边界（DRY/SOLID）**：与 `popup-handler.py` 严格分工——popup-handler 主动查询 + 关闭弹窗；recorder 被动监听 + 落操作流。**不**在 recorder 内部调用 `cleanup_all()`（避免隐式副作用）。两个工具通过 SOP 串联：`cleanup_all()` → `start_recording()`。
- **资源所有权**：全程遵守 §1.3 铁律，**禁止** `browser.close()` / `context.close()` / `page.close()`；监听器通过 `page.remove_listener()` 注销（不靠进程结束清理）。
- **同步面**：本次横跨 8 类文件改动。
- **版本策略**：新增用户可见工具 + 协作模式 B 工具化 = minor bump `2.14.0 → 2.15.0`。

## v2.16.0 抓包失败 7 层诊断 + cURL 快速帮助

- **v2.16.0**：真实用户复盘（2026-07）发现两个体系缺口——
  - **缺口 A**：抓包失败不是单点原因，而是 7 个相互叠加的坑。最关键的 3 个是：(1) Playwright/DrissionPage 默认只 attach 到顶层 page Target，SPA 内部 fetch 走了 worker/iframe 副 target → `Network.requestWillBeSent` 根本不发到主 session；(2) Chrome 150 起新增 `--remote-allow-origins` Origin 校验 → 任何非浏览器/扩展来源的 WebSocket 直连 9222 都会被 403 Forbidden；(3) AI 自己 `Target.createTarget` 拉起了新 tab，但最终在 attach 时挑中了自己创建的那个 tab（EAF22CC9AD995855B401），而不是用户 Chrome 自己的结果页 tab（4252F91C4CC929918E03），所以一直收不到 POST。
  - **缺口 B**：用户提供 cURL 时缺少快速帮助 SOP——cURL 是已知接口最高价值告知形式，但 §3.0.1 模式 C 只说"用户告知接口 → 直接录入"，没有 cURL 12 项快速帮助清单和 cURL → 代码转换 SOP。
- **关键修复**：
  - **A**：新增《爬虫工具与抓包规范》§3.9 漏抓诊断 7 层决策树 + §3.9.2 切换模式前 6 问自检（强门禁）；§3.7 Chrome 启动命令加 Chrome 150+ `--remote-allow-origins=*` 必传警告；§3.5/§3.6 新增"禁止 `Target.createTarget`"反模式；§1.1 加 HTTPS 解密验证清单（L4）；§1.1.1 新增抓包过滤器反向风险与请求类型可见性表（L5/L6）。
  - **B**：新增《爬虫分析规范》§3.0.7 cURL 12 项快速帮助清单 + §3.0.8 cURL → 代码转换 SOP 提示。
- **实战案例**：attach 真实 page target（4252F91C4CC929918E03）+ 不驱动表单 + 让用户手动点一次触发 POST → 1 秒内抓到 200 响应。
- **关键决策**：cURL 优化是**分析能力扩展**而非**新工具脚本**——❌ **不**引入独立 `curl_parser.py` 工具。
- **铁律新增**：`mcpowers-crawler-reverse/SKILL.md` 第 11 条铁律——"抓不到 ≠ 不存在"。
- **版本策略**：minor bump `2.15.0 → 2.16.0`。

## v2.17.0 模块产物封装形式标准化

- **v2.17.0**：真实用户复盘（2026-07）发现模块产物**复用体验差**。
- **4 类核心修复**：
  - **类式封装**：`functions.py` → `client.py`，默认主入口类（除非纯算法/字典常量）。`ModuleClient` 提供 `build_request` / `do_request` / `parse_response` + `request_and_parse` 便捷方法。
  - **请求与解析分离**：`do_request` 只发请求返回原始 Response，`parse_response` 只解析响应。
  - **零前置参数调用**：业务调用方法只接收业务参数（`item_id` 等），token / cookie / sign / nonce / timestamp 模块内部按生命周期分类自洽生成。
  - **`quick_test.py` 手动验证入口**：每个模块必备 `if __name__ == "__main__":` 模式；演示 3 类典型用法（业务要数据 / 业务要报文 / 业务要原始响应）。
- **顶层分析文件改中文**：`ANALYSIS_PLAN.md` → `分析计划.md`、`05-case-study.md` → `05-案例沉淀.md`。
- **v2.17.0 用户二次确认扩展**：**所有分析文件名 + 子目录强制中文**。
- **关键决策**：
  - ✅ 类式 + 请求解析分离 + 零前置参数 + quick_test 都是**用户可见约束**，必须写进规范
  - ✅ extract 技能产物也必须遵循新约定
  - ⚠️ 不强制改造 `popup-handler.py` / `user-action-recorder.py` 等工具脚本
  - ✅ **v2.17.0 二次确认**：子目录 + 内部文件**全部强制中文**（不只是顶层推荐）
  - ⚠️ Python 模块名（`client.py` 等）保留英文
- **同步面**：本次横跨 8 个文件改动。
- **版本策略**：minor bump `2.16.0 → 2.17.0`。

## v2.18.0 DrissionPage 全场景默认化

- **v2.18.0 真实用户复盘（2026-07-28）**：用户反馈"DrissionPage 自动化逆向分析能力更强、占用更低、代码量更少，希望默认优先用"——本质是分析/封装脚本的**代码量**痛点。
- **关键修复（11 类文件改动）**：
  1. 规范主表第一行 Playwright → **DrissionPage**，Playwright 降为 fallback 路径
  2. §2.1 接管语法对照表新增（`connect_over_cdp` → `ChromiumPage(addr_or_opts=ChromiumOptions().set_local_port(9222))` 等）
  3. 漏抓 7 层 DrissionPage 重新映射
  4. `popup-handler.py` DrissionPage 适配
  5. `user-action-recorder.py` DrissionPage 适配
  6. 8 reverse SKILL.md 工具栈 DrissionPage 化
  7. `check-readme-sync.sh` §14 新增 23 校验项
  8. CLAUDE.md / README.md 同步
  9. plugin.json + marketplace.json × 2 version bump
- **关键决策（YAGNI 守边界）**：
  - ❌ **不**为 Chrome 150+ 兼容专门写 `chrome_starter.py` 工具脚本
  - ❌ **不**为 popup-handler 8 类弹窗字典 200+ selector 写"自动迁移工具"
  - ❌ **不**保留双实现 wrapper 抽象层
  - ✅ **保留 Playwright fallback 路径**（5 类弱场景：Cloudflare / 海外 SPA / 复杂 Shadow DOM / iframe 嵌套 / 重度反指纹）
  - ✅ **保留** bb-browser + popup-handler + user-action-recorder 3 工具
- **关键风险**：Chrome 150+ `--remote-allow-origins=*` 必传、Chrome 136+ 独立 user data dir 必传、DrissionPage 单点维护风险
- **铁律新增**：`mcpowers-crawler-reverse/SKILL.md` 铁律 #8 改写；§3.5/§3.9.4 新增 v2.18.0 反模式
- **同步面**：横跨 18 文件 / ~1500 行 / 14/14 校验全绿
- **版本策略**：minor bump `2.17.0 → 2.18.0`

## v2.18.1 DrissionPage 反检测描述精准化

- **v2.18.1 真实复盘**（commit eb638a8 → 用户反馈"上网确认"）：v2.18.0 §2.1/§7.2 主表第 1 行 DrissionPage 描述为"**内置接管浏览器 + 内置反检测**"——**与公开实测不符**。CSDN 2026-07 三大框架对比 / Python 爬虫三剑客对比（2026-04）等公开资料显示：DrissionPage 优势在 5秒盾/Turnstile **自动化通过率**，**反指纹能力（`navigator.webdriver` 泄露）反而弱于 Playwright + rebrowser / puppeteer-real-browser**。
- **关键修复**：3 处描述精准化 + 4 个实测参考链接
- **关键决策**：
  - ✅ **保留** v2.18.0 主要技术决策
  - ✅ **不引入** rebrowser / puppeteer-real-browser 作为新主工具
  - ❌ **不立即**跑真实接管链路 1 次验证
- **铁律强化**："**未上网确认的事实禁止写进主表 description**"
- **版本策略**：patch bump `2.18.0 → 2.18.1`

## v2.18.2 真实接管链路实测暴露 4 个 bug

- **v2.18.2**：v2.18.1 §3.7 留下的"真实接管链路 1 次实测"任务，在用户主动验收驱动下落地。**关键发现：4 个真实 bug**：
  1. **`user-action-recorder.py:506 duck-type bug**（最致命）——`callable()` 判断错误导致 DrissionPage 接管模式 `start_recording()` 整个函数完全无法使用
  2. **`stop_recording` 同样 duck-type bug**
  3. **`page.actions.wheel` API 误用**——DrissionPage 没有 `wheel`，正确是 `page.actions.scroll`
  4. **`popup-handler.py` POPUP_SELECTORS 漏配 notification**——补 2 行 selector
- **顺带修复 2 个小 bug**：`stop_recording` 缺 HAR buffer flush；`replay_actions` 缺防御性读取
- **seen_elements 去重副作用不修（Bug 5）**：真实站点不用通用 `.popup` 类名，字典设计合理，**不改**
- **完整链路实测环境**：
  ```
  Chrome 150.0.7871.187 + --remote-allow-origins=* + --user-data-dir=<独立> + 9222
  + ChromiumOptions().set_local_port(9222).set_user_data_path(<独立>)
  + ChromiumPage(addr_or_opts=co)
  + page.listen.start()
  + popup-handler 检测 8/8 + 分类 8/8
  + user-action-recorder start/stop 接管模式真实可用
  + page.actions.scroll() 正确 API
  ```
- **关键经验**：
  - **代码读起来对 + 文档自洽 ≠ 真实可用**
  - **`hasattr(page, "listen")` 优于 `hasattr + callable`**
  - **DrissionPage API 名 ≠ Playwright**
  - **字典覆盖率必须配套测试**
- **铁律新增**：新工具/新接管语法 → **必跑** 1 次接管链路口令
- **版本策略**：patch bump `2.18.1 → 2.18.2`

## v2.19.0 逆向工作区与 Web 协作会话强制起手式 + 指纹审计

- **v2.19.0**：v2.18.2 修完 4 个 bug 后真实用户复盘发现**新的体系缺口**（A 执行顺序 / B Web 协作模式 / C OS 实现 / D 指纹真实性 / E JS 证据 / F 代码注释）。
- **关键修复（11 类文件改动）**：
  1. 新增唯一公开起手工具 `reverse-analysis-session.py`（init / web-start / web-stop / status）
  2. 固定状态机 `WORKSPACE_READY → ENV_READY → BROWSER_READY → FINGERPRINT_READY → MONITORING → STOPPED`
  3. 浏览器指纹审计（阻断 / 警告 / 不可本地证明 三档）
  4. JS 监控最小可用（4 类高价值通道）
  5. `user-action-recorder.py` 强化脱敏（DOM + HAR + Body）
  6. 中文注释强制
  7. 4 类规范收敛（主册 + 工具册 + Web 册 + spec-index）
  8. 3 类技能收敛（路由器 / crawler-reverse / reverse-web）
  9. 物理门禁 §15（必备文件 / 反模式残留 / 唯一入口）
  10. CLAUDE.md / README.md
  11. 版本号三处一致
- **关键决策（YAGNI 守边界）**：
  - ❌ **不**为 Chrome 150+ / 反指纹引入新工具脚本
  - ❌ **不**为 JS 监控引入独立 `js_monitor.py`
  - ❌ **不**为指纹检查引入外部 IP/TLS 服务
  - ❌ **不**改 6 个 App/小程序专项的工具栈
  - ❌ **不**改 `popup-handler.py` 的 8 类弹窗字典
  - ✅ **保留** v2.18.x DrissionPage 接管语法 + bb-browser + Playwright fallback
  - ✅ **保留** v2.17.0 类式封装
  - ✅ **保留** 外部资源所有权铁律
- **关键风险与未实测项**：真实接管链路 v2.19.0 仍未跑；JS 监控基于页面 JS 包覆；指纹审计 ≠ 反指纹
- **铁律新增**：铁律 12/13 强制 `init → web-start → web-stop` + 指纹门禁；铁律 6 资源所有权铁律重新声明
- **同步面**：横跨 12 类文件
- **版本策略**：minor bump `2.18.2 → 2.19.0`

## v2.20.0 项目独立端口

- **v2.20.0**：v2.19.0 把逆向起手式收敛为单状态机后，真实多任务并行场景暴露新的体系缺口（端口硬编码冲突 / 端口与工作区未绑定 / DrissionPage `set_local_port(0)` 兼容性未确认）。
- **关键修复（8 类文件改动）**：
  1. 新增 `pick_free_port(preferred, max_attempts=100) -> int`（socket.bind 0 + 端口池 9222..9300 fallback）
  2. 新增 `resolve_port(workspace, explicit_port)`（CLI > JSON > 重新分配）
  3. `init` 阶段决定端口并写入《会话状态.json》
  4. CLI 默认值语义改 None
  5. `run_web_session` 状态机在 ENV_READY / BROWSER_READY 阶段写 `chrome_port` 字段
  6. 规范文档占位符化（§3.7.1 新增端口独立分配 SOP）
  7. 物理门禁 §16
  8. CLAUDE.md + README.md + 3 版本文件
- **关键决策（YAGNI 守边界）**：
  - ❌ **不**拆出独立 `chrome-starter.py` / `port-manager.py`
  - ❌ **不**把端口池写成配置文件
  - ❌ **不**改 DrissionPage `set_local_port` 行为
  - ❌ **不**改 `popup-handler.py` / `user-action-recorder.py`
  - ✅ **保留** v2.19.0 的 `_write_state` extra 字段机制
  - ✅ **保留** 外部资源所有权铁律
- **关键风险与未实测项**：真实并行场景 v2.20.0 仍未跑；bind 0 全平台理论兼容；端口冲突的语义边界
- **铁律新增**：铁律 14（v2.20.0）端口必须由 init 自动分配
- **同步面**：横跨 8 类文件
- **版本策略**：minor bump `2.19.0 → 2.20.0`

## v2.21.0 Web 会话派生产物自动生成 + App 录制选型调研

- **v2.21.0**：用户原始诉求——"强制第一时间录制 → AI 创建浏览器/App → 用户操作 → 生成可复用 AI 自动化代码 → 录制期抓包 → 风控弹窗处理 → AI 后续全自动逆向"。v2.19.0 + v2.20.0 已建立"逆向工作区 + Web 协作会话"链路，但 `web-stop` 后仍缺 4 类衔接：
  - 缺口 A：录制 → 类式 client 种子
  - 缺口 B：录制 → 目标接口自动定位
  - 缺口 C：录制 → 响应样本自动归档
  - 缺口 D：App 录制方案未经对照
- **关键修复（11 类文件改动）**：
  1. 新增 `session-artifacts-generator.py`（~650 行）
  2. 六维评分（100 分制）：响应码 200（20）+ 操作触发（25）+ 业务 JSON 字段（20）+ 反爬特征（10）+ 静态 vs 动态参数（15）+ 重复次数（10）
  3. 响应样本 envelope：每个 top10 接口 1 个最大且可解析 JSON 的脱敏 preview
  4. v2.17.0 类式 `client.py` / `quick_test.py` 种子
  5. `reverse-analysis-session.py` 集成（在 `web-stop` 末尾调 `run_artifacts_generation`）
  6. 新增 `tests/session-artifacts-generator-verify.py`（~400 行）
  7. `check-readme-sync.sh` §17 新增（~40 项）
  8. `plugin-verify.sh` §7.6 新增
  9. 3 份主规范同步（《爬虫分析规范》§3.11 App 录制选型调研 / 《爬虫工具与抓包规范》§8.8 / 《爬虫Web逆向规范》）
  10. 3 个 SKILL.md 同步
  11. CLAUDE.md / README.md / 3 版本文件
- **关键决策（YAGNI 守边界）**：
  - ❌ **不接** Playwright codegen 主链路
  - ❌ **不接** Appium / Frida + 自研 / Accessibility Service（v2.21.0 仅做调研）
  - ❌ **不接** Stagehand / browser-use / Steel-browser
  - ❌ **不扩张** HAR body 存储范围
  - ✅ **保留** v2.18.0 DrissionPage 默认接管
  - ✅ **保留** v2.19.0 资源所有权铁律
  - ✅ **保留** v2.20.0 项目独立端口
  - ✅ **保留** v2.17.0 类式封装约定
- **关键风险与未实测项**：真实并行场景 + 真实接管链路 v2.21.0 仍未跑；HAR body_preview 1024 字符上限；request/response 配对保守；自动 lifecycle 分类是线索；真实 top10 排名仍需人工确认；App 三方案没有 PoC
- **铁律新增**：`mcpowers-crawler-reverse/SKILL.md` 铁律 15（v2.21.0）；`mcpowers-reverse-web/SKILL.md` 反模式 +3 条；`mcpowers-crawler-reverse/SKILL.md` 反模式 +4 条
- **同步面**：横跨 12 类文件
- **版本策略**：minor bump `2.20.0 → 2.21.0`

---

## v2.21.2 标准化 Web 浏览器监控模板

- **背景**：用户反馈核心痛点——"AI 即便用对 DrissionPage，也不一定一开始就配置正确"。典型 7 类坑：`listen.start` 时机、循环退出、响应体判空、`postData` 保护、Console 超时、`ChromiumOptions` 配置、截图风控容错。需求是把一个已实测验证过的成熟模板抽出来作为工具库标配，让 AI 直接调用避免从零配置。
- **关键修复**：
  1. 新增 `web_monitor_template.py`（~190 行，含完整 docstring + 中文注释）
  2. 公开契约：`create_browser(headless=False)` + `monitor(url, output_dir='.', headless=False, max_packets=200)`
  3. 顶部 docstring 写明"资源所有权铁律"边界：本工具关闭自身创建的浏览器，不接管用户已有 Chrome
  4. `__all__` 导出 `monitor` / `create_browser`，允许其他脚本 `from web_monitor_template import monitor`
  5. `pathlib.Path` 处理 `output_dir` + `mkdir(parents=True, exist_ok=True)`，避免硬编码路径
  6. 自验证暴露并修复 1 个真实 bug：`tab.cookies()` / `tab.run_js(...)` 缺 try/except 保护——目标页 JS 执行失败时整个 monitor 抛致命异常。修复后 `monitor()` 在异常路径下也能正常返回 `result.json`
  7. 自验证步骤：语法 → 模块导入 → 端到端 httpbin.org（捕获 24 个请求 + result.json 11.6KB + screenshot.png 116KB）→ 异常路径（不可达 URL + JS 异常保护）
- **关键决策（YAGNI 守边界）**：
  - ❌ **不接** Playwright / bb-browser / Selenium（与 v2.18.0 DrissionPage 默认化约束一致）
  - ❌ **不接** v2.19.0 状态机（本工具是开箱即用模板，不替代 `web-start` 的工作区 + 协作会话 + 派生产物状态机）
  - ❌ **不接** v2.20.0 项目独立端口（本工具用 DrissionPage 自管浏览器，不涉及端口分配）
  - ❌ **不接** v2.21.0 派生产物（本工具输出 `result.json` + `screenshot.png`，不生成 `client.py` 种子）
  - ❌ **不修改** reverse-web / crawler-reverse / 其他 reverse-* 技能 SKILL.md（用户明确要求"其他的技能流程约束等等都是原来的"）
  - ❌ **不修改** 9 份规范正文（仅 §7.2 工具对照表新增 1 行）
  - ❌ **不创建** 新目录（脚本归口 `skills/mcpowers-crawler-reverse/scripts/`，与现有 4 个脚本并列）
  - ✅ **保留** v2.18.0 DrissionPage 默认接管
  - ✅ **保留** v2.19.0 资源所有权铁律（顶部 docstring 写明边界）
  - ✅ **保留** v2.20.0 项目独立端口（无冲突）
  - ✅ **保留** v2.21.0 派生产物（本工具不与之重叠）
- **关键风险与未实测项**：
  - 自验证只在 `httpbin.org/anything`（GET，无 cookies）+ 不可达 URL 两个场景跑通
  - 真实逆向目标（如电商 / 金融风控站点）的 JS 异常路径未实测（仅 try/except 防御）
  - 截图风控容错仅靠 `try/except`，未对截图失败做告警日志
  - `headless=True` 模式自验证未实测（端到端用了 `headless=False`）
  - 反指纹场景下本工具仍泄露 `navigator.webdriver`（与 DrissionPage 默认行为一致，详见 §7.2）
- **铁律新增**：无（仅修复 1 个自验证暴露的真实 bug）
- **同步面**：6 类文件 — 1 新增脚本 + 3 版本/插件元数据 + 1 规范工具对照表 + 2 顶层维护文档
- **版本策略**：patch bump `2.21.1 → 2.21.2`（CLAUDE.md 修复/新增工具脚本 → patch）

---

## 维护说明

- **追加规则**：每个 release 后在文末追加新版本段，不再修改历史段。
- **链接规范**：新版本段必须含 4 类固定子标题：背景 / 关键修复 / 关键决策（YAGNI 守边界）/ 关键风险与未实测项 / 铁律新增 / 同步面 / 版本策略。
- **权威源关系**：本文件**不**是规范权威源，仅作为维护者复盘存档；具体规则必须去对应 `技术规范/*.md` / `AI操作规范.md` 查阅。
- **禁止**在 `CLAUDE.md` / `README.md` 复制本文件任何段；顶层文档只允许 1 行相对链接。
